# Optional: enables the natural-language "AI Coach" tab.
# Get a key at https://console.anthropic.com . The app works fully WITHOUT this
# (it falls back to the free, transparent rules engine).
ANTHROPIC_API_KEY=
